# Notebook Guide

The reproducible pipeline is kept in `src/`. If you want to demonstrate the project in Jupyter, run the four scripts in order and use `data/processed/fraud_analysis_data.csv` for exploration.
